using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoalPowerPlant : PowerPlant
{

    new public double maxPower = 100;
    new public double carbonFootprintIntensity = 100;
    new public double carbonFootprintRadius = 50;
    new public double maintenanceCost = 10;
    new public double initialCost = 100;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
