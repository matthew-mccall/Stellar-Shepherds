using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NuclearPowerPlant : PowerPlant
{
    new public double maxPower = 100;
    new public double carbonFootprintIntensity = 10;
    new public double carbonFootprintRadius = 5;
    new public double maintenanceCost = 60;
    new public double initialCost = 1000;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
